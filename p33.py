#Question: Assign two different string values to variables first_name and last_name,
#then use the + operator to combine them into a new variable called full_name with a space in between. 
#Print full_name. 
s1 =" rashi"
s2 = "dang"
s3 = s1+s2
print (s3)